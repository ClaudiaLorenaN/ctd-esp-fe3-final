import React from "react";

const ExitMessage = ({usuario}) => {
  return (
    <>
      <>
        <h4>Gracias {usuario.name}, te contactaremos cuando antes vía mail {usuario.email}".
        </h4>
      </>
    </>
  );
};

export default ExitMessage;